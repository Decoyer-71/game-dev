# 무공 목록 화면 설계안 — Phase 4 첫 화면

**작성 2026-08-09** · 브랜치 `ui/combat-view` · 근거: 작업공간 `CLAUDE.md` §9(비용 판정표) · 프로젝트 `CLAUDE.md` §1-1(Core/Unity 층 분리)

> **✅ 2026-08-09 사용자 승인.** 승인 뒤 0번(Core)부터 착수했다.

---

## 0. 왜 이 화면부터인가

사용자가 셋 중 **무공 목록**을 골랐다(2026-08-09). 근거는 셋이다:

1. **데이터가 이미 전부 있다** — 무공 138종 · 형태소 88자 · 파서 · 유도 수치. **새로 만들 게임 로직이 0** 이고 Unity 층은 읽어서 그리기만 한다
2. **에디터 작업이 가장 작다**(§9 — 에디터 작업은 *"가장 비쌈 · 사용자만 가능"*)
3. ⭐ **형태소 체계가 눈에 보이는 첫 자리다.** *"이름 넉 자에서 수치가 풀린다"* 는 이 프로젝트의 대원칙(정의서 §0)을 지금까지 Sandbox 콘솔로만 확인했다

⚠ 전투 화면(범위 대가 T 확정에 필요한 것)은 4대4 + 진형까지 그려야 해서 더 무겁다. **이 화면으로 UI 파이프라인(한글 · 런타임 조립 · Core 바인딩)을 먼저 뚫고** 전투 화면은 그 위에 올린다.

---

## 1. ⚠⚠ 진짜 관문은 한글이었다 — 조사가 한 번 뒤집혔다

`docs-lookup` 이 낸 결론을 `verify` 가 **패키지 소스로 반증**했다. 뒤집힌 것을 남긴다.

### 1-1. ⛔ 기각 — TMP + OS 폰트 런타임 SDF 굽기

```csharp
var osFont = Font.CreateDynamicFontFromOSFont("Malgun Gothic", 32);
var fontAsset = TMP_FontAsset.CreateFontAsset(osFont, 90, 9, GlyphRenderMode.SDFAA, 1024, 1024, AtlasPopulationMode.Dynamic, true);
```

*"에디터에서 폰트를 안 구워도 한글이 동적으로 채워진다"* 는 안이었다. **성립하지 않는다.**

- `CreateFontAsset(Font, ...)` 는 내부에서 `FontEngine.LoadFontFace(font, ...)` 를 부르고, 실패하면 *"Make sure **Include Font Data** is enabled in the Font Import Settings"* 를 남긴다
- `Include Font Data` 는 **에셋으로 임포트된 폰트 파일의 Import Settings** 항목이다. 그런데 `Font.CreateDynamicFontFromOSFont` 가 만든 것은 **OS 폰트 이름만 든 런타임 객체**라 Import Settings 자체가 없다
- → 요구 조건을 **구조적으로** 만족할 수 없는 조합이다

### 1-2. ⛔ 기각 — TMP Essentials 임포트를 코드로 우회

`TMP_Text` 는 새로 만들어질 때마다 `LoadDefaultSettings()` 를 타고, 그 안에서 `TMP_Settings.instance` 를 역참조한다. `instance` 는 `Resources.Load<TMP_Settings>("TMP Settings")` 로 찾는데 **그 에셋을 만드는 것이 바로 Essentials 임포트**다. 없으면 `null` 을 그대로 돌려주므로 **커스텀 폰트를 명시 할당해도 `NullReferenceException` 을 낸다.**

→ **TMP 를 쓰는 순간 임포트(클릭 2회)가 전제조건이 된다.**

### 1-3. ✅ 채택 — 레거시 `UnityEngine.UI.Text` + OS 동적 폰트

```csharp
Font korean = Font.CreateDynamicFontFromOSFont("Malgun Gothic", 16);
text.font = korean;
```

| | |
|---|---|
| **에디터 클릭** | **0회** |
| **커밋할 폰트 파일** | **0개** — OS 폰트를 참조만 한다(라이선스 문제 없음) |
| 근거 | `Font.CreateDynamicFontFromOSFont` 는 Unity 6000.0 에 **존재하고 deprecated 가 아니다**(공식 레퍼런스). `UI.Text` 도 ugui 2.0.0 에서 **`[Obsolete]` 표기가 없다**(패키지 소스 확인) |

⚠ **대가** — `UI.Text` 는 SDF 가 아니라 **확대하면 흐려진다.** 고정 크기 텍스트 목록에는 무해하지만, 나중에 확대/축소가 필요해지면 TMP 로 승격해야 한다.
⚠ **배포 시** — 빌드 대상 PC 에 그 OS 폰트가 없으면 실패한다. `맑은 고딕` 은 Windows 기본 내장이라 위험이 낮지만, **폰트 이름 문자열이 로케일에 따라 다를 수 있다.** 그래서 아래 §4 에 **후보를 순서대로 시도하고 실패를 로그로 남기는** 절차를 넣는다.

### 1-4. ⏸ 보류 — TMP 정식 경로

Essentials 임포트(클릭 2회) + 한글 `.ttf` 를 `Assets/Fonts/` 에 넣고 `Include Font Data` 를 켠 뒤 런타임 `CreateFontAsset`.
**재검토 조건**: 텍스트 확대/축소가 필요해지거나, `UI.Text` 의 렌더 품질이 실제로 문제가 됐을 때.
⚠ 이 경로는 **폰트 파일을 저장소에 넣어야 하므로 라이선스를 먼저 본다** — 맑은 고딕은 재배포 불가라 OFL 폰트(나눔고딕·Pretendard 등)를 받아야 한다.

---

## 2. 사용자 에디터 작업 — **목표 0회**

§9 가 *"Unity 에디터 작업 = 가장 비쌈 · 사용자만 가능"* 이라 못박았으므로, 이 화면의 설계 기준은 **클릭 수**다.

| 방법 | 사용자 작업 |
|---|---|
| 씬에 Canvas·Text 를 손으로 배치 | ⛔ 수십 회 |
| 프리팹을 만들고 인스펙터로 연결 | ⛔ 수십 회 |
| **빈 GameObject + 스크립트 첨부** | ⚠ 4~5회 |
| ✅ **`[RuntimeInitializeOnLoadMethod]` 로 자동 부팅** | **0회 — Play 만 누른다** |

`RuntimeInitializeOnLoadMethod(AfterSceneLoad)` 는 공식 문서가 *"에디터에서 Play 모드에 들어갈 때도 같은 호출이 보장된다"* 고 명시한다. **씬에 아무것도 없어도 스크립트가 스스로 Canvas·EventSystem·목록을 만든다.**

⚠ **함정 하나 등재** — 도메인 리로드를 끈 상태에서 Play 를 재진입하면 정적 상태가 남아 **중복 생성**될 수 있다. 부팅 시 이미 만든 루트가 있으면 지우고 다시 만드는 가드를 넣는다.
⚠ 입력 시스템 — 이 프로젝트는 `activeInputHandler: 2`(Both, `ProjectSettings.asset:679` 실측)라 구식 `StandaloneInputModule` 도 동작한다. **`EventSystem` 이 씬에 있는지 먼저 찾고 없을 때만 만든다.** 두 입력 모듈을 같은 `EventSystem` 에 동시에 얹지 않는다.

---

## 3. 화면 내용

**한 화면 · 좌우 2단.** 스크롤은 왼쪽 목록에만 있다.

```
┌─ 무공 138종 ──────────────┬─ 상세 ─────────────────────────┐
│ [계층▾][유형▾][성향▾] 검색 │  마한중참  馬寒重斬              │
│ ─────────────────────────── │  천마신교 · 전승무학 · 검 · 마도 │
│ ▸ 절정검법   강호무학 검    │                                  │
│ ▸ 유독척     소문파  비도   │  이름이 푸는 수치                │
│ ▸ 마한중참   전승   검      │   마(魔) 극한경지 공격+2.5 방무25 │
│ ▸ 만우쾌사   전승   비도    │   한(寒) 수식     치명배율+0.3    │
│   …                         │   중(重) 무공형태 공격+1 속도−2   │
│                             │   참(斬) 공격방식 공격+2          │
│                             │  ─────────────────────────────   │
│                             │  합계  공격 5.50 · 기력 12       │
│                             │  범위 단일 · 열 후열 · 규칙 없음  │
└─────────────────────────────┴──────────────────────────────────┘
```

⭐ **오른쪽이 이 화면의 존재 이유다.** 글자 하나하나가 어떤 수치를 넣었는지 보이고, 그 합이 아래에 나온다 — *"수치의 출처는 오직 이름"* 이 화면에서 증명된다.

**⚠ 1단계에서 넣지 않는 것** (범위를 좁힌다)
- 정렬·검색 (필터 3개만)
- 스크롤 관성·애니메이션·색 테마
- 무공 습득·수련 등 **상태를 바꾸는 것 일체** — 이 화면은 **읽기 전용**이다

---

## 4. 구현 형태

### 4-0. ⛔⛔ 먼저 Core 에 구멍이 하나 있다 — `verify` 가 잡았다

**우측 상세를 그리려면 무공을 다시 분해해야 하는데, 지금 API 로는 상당수가 예외를 던진다.**

- `MorphemeParser.Parse(name)` 의 1인자 오버로드는 `kind = null` 로 도는데, 그건 **접미사에서 종류를 읽는 경로**다
- 접미사가 없는 이름은 **호출자가 `ArtKind` 를 명시해야 한다**
- 그런데 **`MartialArt` 는 자기 `ArtKind` 를 보관하지 않았다** — `MartialArtFactory` 가 계산해 쓰고 버렸다
- **실측 추적**: 시안에 쓴 `마한중참` 조차 `Parse("마한중참")` 로는 *"접미사가 없다"* 로 **`ArgumentException`** 이 난다

⭐ **시안으로 고른 예시가 시안대로는 안 돌아갔다.**

**✅ 실측 확정 — 71종이다** (2026-08-09, 변이 시험). ⚠ `verify` 는 *"129/138"* 로 추정했는데 **틀렸다**: 내공·경공 무공은 이름이 `공`·`결`·`보`·`술` 로 끝나 **접미사를 갖기 때문에** 1인자 경로로도 분해된다. 실제로 깨지는 것은 **접미사가 없는 공격 초식 71종**이다. → **추정치를 그대로 문서에 남기지 않는다.**

**→ 1단계 0번 작업 — ✅ 2026-08-09 완료**
- `MartialArt.Kind` 신설 · `MartialArtFactory` 가 `parsed.Kind` 를 실어 보낸다(상성·범위·열과 **같은 통로**)
- **`MartialArtFactory.Decompose(MartialArt)`** 신설 — 화면은 파서를 직접 부르지 않는다. *"강호무학이냐 아니냐"* 로 갈리는 한 줄이 `TryCreate` 와 **한 곳에만** 있어야 갈라지지 않는다
- 회귀 **4건** · `dotnet test` **239/0/0** · ⚠ 변이(원래 계획대로 1인자 호출로 되돌리기)로 **71종 실패를 실제로 재현**해 시험이 공허하지 않음을 확인

**⭐ 그 과정에서 하나 더 나왔다 — 접미사는 강호무학 전용이 아니다.** `신풍양공`(소문파 내공)도 `공` 이 떨어져 본체가 `신풍양` 3자다. 내공은 `공`·`결`, 경공은 `보`·`술` 로 끝나는 것이 작명 관례이기 때문이다.
→ **상세 화면은 접미사 글자에 *"형태소 아님"* 을 표시해야 한다.** 수치가 없는 글자인데 아무 표시가 없으면 *"이 글자는 왜 기여가 없지"* 가 남는다.

### 4-1. 파일

**Unity 층 4개 · 200~350줄 예상 · Core 는 위 0번 한 줄**

| 파일 | 하는 일 |
|---|---|
| `UiBootstrap.cs` | `[RuntimeInitializeOnLoadMethod]` 진입점. 중복 가드 · Canvas/EventSystem 생성 |
| `KoreanFont.cs` | OS 폰트 후보를 **순서대로 시도**하고 전부 실패하면 **로그로 크게 알린다**. ⚠ 조용히 □ 로 두지 않는다 |
| `MartialListScreen.cs` | 좌측 목록 + 필터. `MartialArtCatalog.All` 을 읽는다 |
| `MartialDetailPanel.cs` | 우측 상세. **Core 가 내준 분해 결과를 그리기만** 한다 — 재분해도 재계산도 하지 않는다 |

**⚠⚠ 이 층은 게임 규칙을 갖지 않는다**(프로젝트 §1-1). Core 를 **읽기만** 하고, 수치를 다시 계산하지 않는다 — 화면이 자체 계산을 시작하면 Sandbox 와 값이 갈린다(이 저장소가 §4-9-6 에서 이미 겪은 병).

**⚠ 테스트** — Unity 층은 `dotnet test` 로 못 돈다(UnityEngine 참조). 그래서 **글자별 기여를 만드는 순수 함수는 Core 쪽에 두고 EditMode 테스트를 붙인다.** 화면은 그 결과를 그리기만 한다.

---

## 5. 순서

0. ✅ **Core — `MartialArt.Kind` 노출 + `Decompose` + 138종 전수 테스트** (§4-0). **완료**
1. ✅⚠⚠ **`KoreanFont` + 스모크 화면 — 사용자 확인 통과** (2026-08-09, 스크린샷 확인)

   | 확인한 것 | 결과 |
   |---|---|
   | 한글이 나오는가 | ✅ `[KoreanFont] 'Malgun Gothic' 을(를) 썼다. (후보 1/10)` — **첫 후보에서 잡혔다.** 한글·漢字·ABC·숫자 전부 정상 |
   | 에디터 작업 0 인가 | ✅ 씬에 아무것도 안 만들었는데 `DontDestroyOnLoad` 아래 UI 가 스스로 섰다 |
   | Core 를 읽는가 | ✅ 카탈로그 138종 |
   | 다시 분해되는가 | ✅ `성뇌후격`(전승무학·접미사 없음) — **0번이 고친 버그가 걸리던 자리** |

   → **UI 파이프라인이 뚫렸다.** 한글·런타임 조립·Core 바인딩·재분해가 한 번에 확인됐다.

   ### ⚠ 화면이 드러낸 문제 셋 — 2번에 반영한다

   1. **⛔ 기여 표시가 반쪽이다.** 스모크 화면이 **공격 축만** 찍어서 `성(聖) 공격 0` · `뇌(雷) 공격 0` 으로 나온다.
      실제로 성(聖)은 **방어 +5 · 막기 +25 · 상태저항 +30** 을 넣는데 화면이 그걸 안 보여준다.
      → **§3 이 약속한 *"글자별 기여"* 가 성립하지 않는다.** 이 화면의 존재 이유가 걸린 자리다
   2. **enum 이 영문으로 샌다** — `Fist` · `Single` · `Front`. 비도·단일·전열이어야 한다
   3. **접미사 글자가 목록에서 사라진다** — 지금 표본은 접미사가 없어 안 드러났지만, `신풍양공` 을 열면
      `공` 자가 어디에도 없어 *"이 글자는 어디 갔지"* 가 된다(§4-0 이 이미 지적한 것)

2. ✅ **Core 에 표시 계층을 먼저 만들고 그 위에 목록을 얹었다** (2026-08-09 재정의 → 2026-08-23 구현)

   ⚠⚠ **원래 계획은 *"좌측 목록 138종 + 필터 3개"* 였는데 순서를 바꿨다.** 위 문제 1·2 가
   *"화면이 예쁘지 않다"* 가 아니라 **표시 로직이 테스트가 닿지 않는 곳에 있다**는 문제이기 때문이다.
   Unity 층은 `dotnet test` 가 안 닿으므로(프로젝트 §4 지뢰), **순수 함수로 뽑아 Core 에 두면 검증이 붙는다.**

   ### 2-a. ✅ Core — 표시용 순수 함수 (`Assets/Scripts/Core/Martial/Display/`)

   | 파일 | 하는 일 |
   |---|---|
   | `KoreanNames.cs` | enum → 한글. `Discipline`·`ArtTier`·`Alignment`·`ArtKind`·`AttackScope`·`BattleRow`·`MorphemeCategory`·`ArtLineage`·`AbsoluteRule` **9종** |
   | `StatAxisValue.cs` | 축 하나 = 이름·값·단위. `%p` / `%` / `배` / `스택` / `/턴` 을 구분해 든다 |
   | `ArtStatDeltaDisplay.cs` | `ArtStatDelta` 에서 **0 이 아닌 축만** 뽑는다 |
   | `MorphemeContribution.cs` | 이름 한 토막(형태소 또는 접미사)이 넣은 것 |
   | `ArtBreakdown.cs` | 무공 하나를 화면용으로 편 것. 진입점은 `ArtBreakdown.Of(art)` |

   **⚠⚠ 이름표가 네 곳에 흩어져 있었다** — 계획은 *"Sandbox 에 따로 있다"* 정도로 적었는데 실제로는:
   `Sandbox.DisciplineName` · `Sandbox.Short(Discipline)`(**같은 파일 안에서 중복**) ·
   `ArtCompositionRule.KindName`/`Describe` · `ParsedArtName.KindLabel`. 전부 `KoreanNames` 로 모으고
   그 자리들은 위임만 남겼다.
   → ⚠ **`MorphemeCategory.AbsoluteRule` 이 옛 `Describe` 에 빠져 있어 `?` 가 나오고 있었다**(면·무·쌍·통).
   모으지 않았으면 안 드러났을 결함이다.

   **⚠⚠ 지표 키가 이 문자열로 만들어진다** (`tier.전승무학.유형.비도.평균.10성`). 그래서 옮긴 뒤
   기준선을 대조했다 — **바뀜 0 · 같음 791 · 신규 0 · 사라짐 0.** 한 글자도 안 움직였다.

   **⚠ 계획이 틀린 것 하나** — *"`ArtStatDelta` 는 22축"* 이라고 적었으나 실제로는 **23축**이다
   (`AttackPercent` 가 2026-08-09 범위 대가로 들어왔다). 파생값 `AttackPenalty` 까지 세면 필드는 24개다.
   → ⚠⚠ **`AttackPenalty` 는 표시하지 않는다.** 이미 `Attack` 안에 들어 있는 값이라 같이 찍으면
   `환(−2)` 이 *"공격 −2 · 공격페널티 −2"* 로 보여 **−4 로 읽힌다.**

   **⛔⚠⚠ `verify` 가 잡은 결함 — 회피율에 거짓 단위를 달고 있었다.**
   처음엔 사전 값을 그대로 내고 `회피율 +15%p` 라 적었다. 그런데 엔진은 **상수를 곱한다**:

   | 축 | 사전 값 | 엔진 상수 | 실제 |
   |---|---|---|---|
   | 회피율 | `피·둔·섬 15` | `Combatant.EvasionPointToPercent` = **0.3** | **+4.5%p** (3.33배 과장이었다) |
   | 명중 | `쾌 −2` | `CombatResolver.AccuracyPointToPercent` = **5** | **−10%p** (단위가 아예 없었다) |

   → *"존재하지 않는 값에 그럴듯한 단위를 붙인 것"* 이고, 프로젝트 §1-0 이 등재한 **방어관통 160%** 와
   **같은 형태의 거짓말**이다. 승률이 맞아서 안이 맞다고 착각한 그 사고와 판박이다.
   ⚠⚠ **23축을 전수로 대조해 이 둘만 그렇다는 것을 확인했다** — 나머지 21축은 엔진이 원값을 그대로 쓴다.
   결함 하나를 고치고 나머지를 안 본 채 넘기는 것이 §5-D 가 경계하는 **선택적 관찰**이라 전수로 봤다.
   ⚠ 상수를 베끼지 않고 **엔진 것을 참조**한다. 저쪽이 미검증 초기값이라 움직일 수 있고, 베끼면
   그때 화면만 옛값에 남는다. 테스트도 상수를 **참조해** 대조한다(베끼면 테스트가 결함을 가려 준다).

   **⚠ 종(宗) 2배 규칙을 공유 함수로 뺐다** — `MorphemeParser.HasFormDoubler` / `EffectiveDelta`.
   표시 계층이 규칙을 다시 적으면 **글자별 값과 합계가 어긋나는 화면**이 나온다. 상성·범위·열·종류에서
   네 번 겪은 *"두 곳에 두면 갈라진다"* 와 같은 형태다.

   **회귀 테스트 16건** (`Assets/Tests/EditMode/Martial/ArtBreakdownTests.cs`) — `dotnet test` **255/0/0**.
   ⚠ 계획은 *"기여 항목이 이름 글자 수와 맞는가"* 였는데 **글자 수가 아니라 이어붙인 문자열**로 검사한다.
   접미사가 2자(`신공`)일 수 있어 개수 비교로는 `신풍양공` 의 `공` 이 빠진 것을 못 잡는다.

   ### 2-b · 2-c. ✅ Unity — 좌측 목록 + 우측 상세

   | 파일 | 하는 일 |
   |---|---|
   | `Assets/Scripts/Unity/UiFactory.cs` | 프리팹 없이 uGUI 조각을 찍는 공장(판·글자·줄·스크롤) |
   | `Assets/Scripts/Unity/MartialListScreen.cs` | 좌 필터 3줄 + 목록 · 우 상세. **계산은 한 줄도 없다** |

   - 필터는 **계층 5 · 유형 7 · 성향 3**, 각 줄 맨 앞이 `전체`. 켜진 것에 색을 준다
   - ⚠ 성향 필터를 걸면 **강호무학이 빠진다** — 무공 자체에 성향이 없기 때문이고(익힌 사람을 따른다) 그게 맞다
   - ⚠ 필터로 선택 항목이 사라지면 상세를 비우거나 첫 항목으로 옮긴다
   - **⛔⚠ `docs-lookup` 이 잡은 결함 — 스크롤 입력이 반쪽이었다.** `Viewport` 에 `RectMask2D` 만
     두었는데, 마스크는 **자르기만 하지 레이캐스트 대상이 아니다.** 그래서 항목 사이 틈이나 목록
     아래 빈 자리에서 시작한 드래그·휠이 안 먹는다. 투명한 `Image`(`raycastTarget`)를 얹어 닫았다
      - **⛔ `SmokeScreen` 은 지웠다.** 목록 화면이 같은 셋(한글·Core·재분해)을 더 넓게 태운다.
     둘을 남기면 *"어느 쪽을 봐야 하는가"* 가 생긴다

3. ⚙ **Unity 확인 — 1차 통과, 레이아웃 결함 둘을 고쳤다** (2026-08-23, 스크린샷 확인)

   ✅ **화면은 섰다** — 138종 목록 · 필터 3줄 · 상세(글자별 기여표 · 합계 · 메타) 전부 렌더됐고
   `[KoreanFont] 'Malgun Gothic'` 첫 후보. `절정검법` 의 정(正)이 **명중 −10%p** 로 떠서
   **환산이 실제로 화면에 닿은 것**까지 확인됐다.

   ### ⛔⚠⚠ 결함 1 — 필터 줄이 세로로 부풀어 목록을 짓눌렀다

   필터 3줄이 좌측 높이를 나눠 먹어 **138종이 8줄만 보였다.**

   원인은 `HorizontalLayoutGroup.childForceExpandHeight = true` 다. 이 값은 *"자식을 줄 높이에
   맞춰 늘린다"* 로만 읽히는데, uGUI 는 그때 **자식들의 flexible 을 최소 1 로 올려 합산한 뒤
   그 그룹 자신의 `flexibleHeight` 로 부모에게 보고한다.** 즉 **안쪽 정렬 지시가 바깥쪽 크기
   협상까지 바꾼다.**
   ⚠⚠ `LayoutElement` 로 높이를 못박아도 소용없었다 — 거기 적은 것은 `preferredHeight`·`minHeight`
   뿐이고 **`flexibleHeight` 는 미설정(−1)으로 남아** 그룹 계산값이 그대로 쓰인다.
   → `element.flexibleHeight = 0` 한 줄로 닫았다.

   ### ⛔ 결함 2 — 접미사 표기가 카테고리 칸을 넘쳤다

   `접미사(형태소 아님)` 이 130px 칸을 넘겨 줄이 어긋났다. 카테고리 칸은 `공격방식` 같은 4자 자리다.
   → 카테고리는 **`접미사`**, *"형태소 아님"* 은 **의미 칸**으로 옮겼다(`공격 무공 · 형태소 아님`).
   열 폭도 실측에 맞췄다 — 카테고리 130→**100** · 의미 110→**180**.

   ⚠ **필터 줄이 좌측 폭을 넘칠 뻔했다.** `Width()` 가 최소폭까지 못박으므로 넘치면 줄어드는 게
   아니라 **패널 밖으로 삐져나온다.** 어림으로 가장 긴 계층 줄이 494px · 가용 544px 이라
   50px 여유를 두도록 버튼 폭 계산을 줄였다.

   ### ⛔⚠⚠ 결함 3 — 성향이 없는 이유가 셋인데 한 가지 설명만 붙였다

   `Alignment == null` 인 무공이 **17종인데 이유가 서로 다르다.** 화면은 전부
   *"익힌 사람을 따름"* 이라 적어 **8종에 거짓말**을 하고 있었다.

   | 계층 | 종수 | 진짜 이유 | 화면 문구 |
   |---|---|---|---|
   | 강호무학 | 9 | 무공에 성향이 없고 **익힌 사람**을 따른다 | `익힌 사람을 따름` |
   | 대문파·세력(**제천성**) | 4 | **정·사·마를 다 받는 유일한 세력**이라 성향 배타가 풀려 있다(정의서 §5-5-b) | `가리지 않음` |
   | 절대경지 | 4 | **기연으로만** 얻고 성향이 없다(§5-3) | `없음 (기연)` |

   ⚠⚠ **카탈로그는 정상이었다.** `거후명보`(제천성)의 `null` 은 의도된 값이고 코드 주석이
   그 이유를 이미 적어 두고 있었다. 틀린 것은 **읽는 쪽**이었다 — 같은 `null` 을 세 뜻으로
   쓰는데 화면이 계층을 안 보고 한 문장을 붙였다.
   → `KoreanNames.Of(Alignment?, string none)` 오버로드를 **지웠다.** 부르는 쪽이 문구 하나를
   넘기는 형태로는 애초에 옳게 말할 수 없다.

   ### ⛔ 결함 4 — 축 셀이 두 줄로 깨졌다

   `성뇌후격` 의 성(聖)이 `방어`/`+5`, `막기확률`/`+25%p` 처럼 **이름과 값이 갈라졌다.**
   축 셀의 `minWidth` 가 0 이라 폭이 모자라면 uGUI 가 셀을 줄이고, 줄어든 셀 안에서 글자가 wrap 된다.

   고친 것 셋 —
   ⓐ **`Text.preferredWidth`(한 줄 실측 폭)를 min·preferred 양쪽에 못박아** 줄어들 수 없게 했다
   ⓑ `horizontalOverflow = Overflow` 로 **어떤 경우에도 줄바꿈하지 않게** 했다. 넘치면
      `ScrollArea` 의 마스크가 깨끗이 자른다 — 잘린 것은 눈에 보이지만 두 줄로 깨진 것은 오해된다
   ⓒ **머리 세 칸을 좁혀 기여 칸을 넓혔다** (글자 72→64 · 카테고리 100→88 · 의미 180→**140**),
      좌측 패널도 560→**520**

   그러면서 *"형태소 아님"* 을 **기여 칸으로 옮겼다**(두 번째 이사다). 카테고리 칸에서는 칸을
   넘쳤고, 의미 칸에서는 칸을 넓히느라 기여 칸을 좁혀 **결함 4 를 내가 만들었다.**
   기여 칸이 옳은 자리다 — *"이 글자가 무엇을 넣었는가"* 의 답이 **"아무것도. 형태소가 아니니까"** 라서다.

   **어림 실측** — 글자 행 최대 **332px**(`성뇌후격` 의 성) vs 가용 약 447px · 합계 행 최대
   **545px**(`환창혈만`) vs 가용 약 771px.

   ### ⛔ 결함 5 — 잠금을 안 건 칸이 하나 남아 있었다 (3차)

   `거후명보` 의 접미사 행에서 `형태소 아님 · 수치 없음` 이 **다음 줄로 밀렸다.**
   결함 4 를 고치며 **축 셀만** 잠갔는데, 머리 칸들이 최소폭까지 못박혀 있으니
   **줄어들 수 있는 것이 이 칸뿐**이라 부족분이 전부 여기로 몰린다.

   ⚠⚠ **짧은 `수치 없음`(절대경지 화면)은 안 깨져서 멀쩡해 보였다.** 같은 결함이
   문자열 길이에 따라 보이기도 안 보이기도 한다 — *"한 화면 봤으니 됐다"* 가 통하지 않는다.
   → 잠금을 `LockWidth` 로 뽑아 **빈 칸 표시와 메타 값에도** 걸었다.

   ### ⚠⚠ 내가 안내를 틀렸다 — `환창혈만` 은 전승무학이 아니다

   *"전승무학 + 창"* 이라고 적었는데 **대문파·세력**이다(`MartialArtCatalog.cs:220`).
   혈교가 `SchoolTier.Major` 라 **대문파 무공(환창혈만)과 전승무학(종환화격)을 둘 다** 갖는다.
   **문파 이름만 보고 계층을 짐작하지 말 것.**

   ### ⏭ 2차 확인 — 다시 Play



   #### ⚠ 기대값을 미리 적는다 (§1-A 규율)

   Core 는 `dotnet test` 로 닫혔으므로, 여기서 보는 것은 **조립·레이아웃**뿐이다.
   아래 값은 Unity 없이 Core 를 직접 돌려 뽑은 **실측**이다(스크래치패드).

   | 볼 것 | 기대값 (실측 근거) |
   |---|---|
   | 목록 종수 | **138종**. 필터 `전체·전체·전체` |
   | 가장 긴 목록 줄 | `중뇌양공   대문파·세력 · 내공 · 정` — **22자**. 좌측 폭 560 에 들어가야 한다 |
   | `성뇌후격` 의 성(聖) | **방어 +5 · 막기확률 +25%p · 상태이상 저항 +30%p** ← 문제 1 이 닫혔는지 보는 자리 |
   | `신풍양공` 의 `공` | 목록 맨 아래에 **`접미사(형태소 아님) · 내공 무공 · 수치 없음`** ← 문제 3 |
   | enum 자리 | `비도` · `단일`/`전원` · `전열`/`후열`. **`Fist`·`Single`·`Front` 가 보이면 실패** ← 문제 2 |
   | `만우쾌사` | 만(萬) 이 **총위력 −37.5% · 기력소모 +200%** · 범위 `전원` · **`후열`** · 기력 27 · 합계 명중 **−15%p** |
   | 종(宗) | **`종환화격` 단 1종뿐이다.** 환(幻)이 **공격 −1.5 · 명중 +20%p**(사전값 −0.75·2 의 2배)로 뜨고 `종(宗) — … 2배` 안내가 붙는다 |
| 회피·명중 환산 | **`피현유보`(무당파)를 연다** — 피(避) **회피율 +4.5%p**(사전 15×0.3) · 현(玄) **명중 −5%p · 회피율 +3%p** · 합계 **회피율 +7.5%p**. **`+15%p`·`+10%p` 로 보이면 환산이 빠진 것이다** |
   | 접미사 보유 | **67종 / 138** |

   #### ⚠ 미리 지목하는 위험 — 여기서 깨질 것 같다

   1. **합계 축이 한 줄을 넘칠 수 있다.** 축이 가장 많은 것은 **합계 6축(`환창혈만`)**, 글자 하나로는
      **4축(`제화정참` 의 제)**. 우측 폭이 모자라면 uGUI 가 최소폭까지 줄이며 **글자를 자른다.**
      ⚠ 어림으로는 `환창혈만` 합계가 **약 540px**, 가용 폭이 **약 976px** 이라 들어갈 것으로 **추정**한다.
      다만 **폰트를 실제로 재 본 값이 아니다** — 그래서 위험 목록에 남긴다.
      → `환창혈만`(합계 6축) 과 `제화정참`(글자 4축) 을 열어 잘리는지 본다
   2. **138개를 한 번에 그리는 비용** (§7-2 의 미결). `Text` 138개 + 배경 138개다
   3. **필터를 바꿀 때마다 목록을 통째로 다시 만든다.** 끊기면 가시 영역만 그리는 방식으로 바꾼다

   **⚠⚠ 반드시 할 것** — 목록 첫 항목만 눌러 보면 **강호무학**이라 §4-0 버그를 그냥 지나친다.
   위 표의 `성뇌후격`(전승무학·다축) · `신풍양공`(소문파·접미사) · `종환화격`(종宗) · `피현유보`(회피 환산) **넷을 반드시 연다.**

   ⚠ **목록이 138줄까지 스크롤되는지**(휠·드래그) 함께 본다 — 1차에서는 목록이 눌려 있어 확인되지 않았다.

---

## 6. ⚠ 무엇이 버려지는가 — 미리 적는다

| | 나중에 |
|---|---|
| `KoreanFont` | ✅ **그대로 쓰인다** — 모든 화면이 한글을 쓴다 |
| `UiBootstrap` | ⚠ 실제 게임이 되면 씬·프리팹 구조로 바뀐다. **프로토타입 스캐폴딩이다** |
| 런타임 조립 레이아웃 코드 | ⚠ 프리팹으로 옮기면 버려진다. 다만 **어떤 정보를 어떻게 배치할지**는 남는다 |
| 형태소 기여 계산(Core 쪽) | ✅ **그대로 쓰인다** — 무공 상세는 어느 화면에서도 필요하다 |

**⛔ 기각한 대안 — IMGUI(`OnGUI`)**: 스크롤·버튼이 코드 몇 줄이라 **더 싸다.** 그러나 Phase 4 의 목적이 *"Unity UI 바인딩"* 인데 IMGUI 는 그 목적을 우회하고 **100% 버려진다.** 우회해서 얻는 것이 며칠치 편의뿐이라 채택하지 않는다.

---

## 7. 미결

1. **`UI.Text` 의 렌더 품질이 실제로 견딜 만한가** — 스모크 테스트에서 함께 본다. 못 견디면 §1-4 로 승격
2. **138종을 한 번에 그리면 느린가** — `UI.Text` 138개는 가벼울 것으로 **추정**하나 재 본 적 없다. 느리면 가시 영역만 그리는 방식으로 바꾼다
3. **폰트 이름 문자열** — `"Malgun Gothic"` 이 이 PC 에서 실제로 잡히는지 **미검증**. §4 의 후보 순회가 그것을 흡수한다

---

## 변경 이력

- **2026-08-09** 최초 작성. `docs-lookup` 조사 → `verify` 가 핵심 둘을 반증 → 그 위에 다시 씀
- **2026-08-23** 2차 Unity 확인 — **결함 넷**(필터 줄 부풀음 · 접미사 칸 넘침 · 성향 null 의 세 이유 · 축 셀 두 줄 깨짐)을 고쳤다. 스크롤·필터·환산은 전부 정상 확인
- **2026-08-23** `verify` 가 회피율 거짓 단위를, `docs-lookup` 이 스크롤 레이캐스트 누락을 잡아 둘 다 반영
- **2026-08-23** 2-a(Core 표시 계층) · 2-b·2-c(Unity 목록·상세) 구현. §5 를 결과로 다시 씀 — 계획이 틀렸던 것(22축→23축, 이름표가 네 곳)과 기대값·위험을 함께 적었다
